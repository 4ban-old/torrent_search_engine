+1. Придумать структуру конфиг файла, как описывать какой модуль, функция и атрибуты в любом количестве.

+3. При запуске, если firstStart=True то читает конфиг, запускает то что там, возвращает словарь
    запущенных процессов обратно.
    Если флаг False сделать запуск переданного модуля, функции с атрибутами и добавлением
    этого в глобальный словарь запущенных процессов.
    Функция run работает, но не до конца и без атрибутов
5. Найти исключения для service, ловить ошибку создания сокета, ошибку bindинга.
    Придумать инициализацию остановки, аварийной, или от хоткея.
6. status name name - обработка двух атрибутов или более
